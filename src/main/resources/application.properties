spring.application.name=webapi

# Identidad de la instancia para el demo de Blue-Green.
# Se sobreescribe con la variable de entorno INSTANCE_NAME.
app.instance.name=${INSTANCE_NAME:LOCAL}

# Versión inyectada por Maven (resource filtering) al construir el JAR.
app.version=@project.version@
